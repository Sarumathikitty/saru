#include <stdio.h>
int main(void)
{
  int n,i=0,n1;
  scanf("%d",&n);
  while(n1<n)
  {
    i+=1;
    n1=pow(2,i);
  }
  if(n1==n)
  {
    printf("yes");
  }
  else
  {
    printf("no");
  }
  return 0;
}  
